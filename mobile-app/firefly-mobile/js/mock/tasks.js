/**
 * tasks.js - 任务相关 Mock 数据
 *
 * 该部分数据源正在开发中，暂时返回空数组
 * 实际任务历史存储在 localStorage（运行时状态）
 * 本文件提供初始化数据和辅助方法
 *
 * 使用方式：通过 <script> 引入后，数据挂在 window.TaskMock 上
 */

window.TaskMock = {
  INITIAL_TASKS: [],
  isDeveloping: true
};